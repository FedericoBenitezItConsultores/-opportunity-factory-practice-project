/**
 * @typedef {Object} Person
 * @property {string} name
 * @property {number} age
 */

/**
 * @param {Person} person Este pueden ser los egresos mensuales
 * @returns {string}
 */
export default function sum(person) {
  return person.name;
}
